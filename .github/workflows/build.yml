name: Build App (Windows and Android)
on: [push]

jobs:
    # --- BUDOWANIE NA WINDOWS (.EXE) ---
  windows-build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          pip install flet pyinstaller
      - name: Build EXE Manual
        run: |
          # Używamy bezpośrednio PyInstallera, co omija błąd flet pack
          pyinstaller --noconfirm --onefile --windowed --name "Organizator_Kryminalny" main.py
      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: Windows_App
          path: dist/*.exe

  # --- BUDOWANIE NA ANDROID (.APK) ---
  android-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install Flet CLI
        run: |
          pip install flet
      - name: Build APK
        run: |
          flet build apk
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Android_App
          path: build/apk/*.apk
          
